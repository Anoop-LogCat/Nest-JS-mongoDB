export const keys = {
    jwtKey: "top_secret_key_not_share",
    googleClientID: "Generate a google o-auth api from GCP and provide client ID",
    googleClientSecret: "Generate a google o-auth api from GCP and provide client Secret"
}
