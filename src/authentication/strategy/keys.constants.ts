export const keys = {
    jwtKey: "top_secret_key_not_share",
    googleClientID: "<Generate Google O-Auth Api from GCP and provide client ID>",
    googleClientSecret: "<Generate Google O-Auth Api from GCP and provide ClientSecret>"
}
